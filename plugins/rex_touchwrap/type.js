"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.rex_TouchWrap;

	PLUGIN_CLASS.Type = class rex_TouchWrapType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
