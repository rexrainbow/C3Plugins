"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.rex_TouchWrap;

	PLUGIN_CLASS.Instance = class rex_TouchWrapInstance extends SDK.IInstanceBase
	{
		constructor(sdkType, inst)
		{
			super(sdkType, inst);
		}
		Release()
		{
		}
		OnCreate()
		{
		}
		OnPropertyChanged(id, value)
		{
		}
		LoadC2Property(name, valueString)
		{
			return false;       // not handled
		}
	};
}
