"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_ListCtrl;

	PLUGIN_CLASS.Type = class Rex_ListCtrlType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
