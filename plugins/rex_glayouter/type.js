"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Layouter;

	PLUGIN_CLASS.Type = class Rex_LayouterType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
