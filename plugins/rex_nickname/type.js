"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Nickname;

	PLUGIN_CLASS.Type = class Rex_NicknameType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
