"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.rex_bbcodeText;

	PLUGIN_CLASS.Type = class rex_bbcodeTextType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
