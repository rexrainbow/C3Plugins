"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_TweenTasks;

	PLUGIN_CLASS.Type = class Rex_TweenTasksType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
