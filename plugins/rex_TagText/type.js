"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.rex_TagText;

	PLUGIN_CLASS.Type = class rex_TagTextType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
