"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_NGIO_Medal;

	PLUGIN_CLASS.Type = class Rex_NGIO_MedalType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
