"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_ZSorter;

	PLUGIN_CLASS.Type = class Rex_ZSorterType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
