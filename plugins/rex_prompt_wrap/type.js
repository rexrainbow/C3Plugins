"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_PromptWrap;

	PLUGIN_CLASS.Type = class Rex_PromptWrapType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
