"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Tilt2ArrowKey;

	PLUGIN_CLASS.Type = class Rex_Tilt2ArrowKeyType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
