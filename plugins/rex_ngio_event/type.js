"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_NGIO_Event;

	PLUGIN_CLASS.Type = class Rex_NGIO_EventType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
