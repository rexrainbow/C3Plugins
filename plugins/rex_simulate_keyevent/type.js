"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_SimulateInput;

	PLUGIN_CLASS.Type = class Rex_SimulateInputType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
