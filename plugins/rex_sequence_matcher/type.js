"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_SequenceMatcher;

	PLUGIN_CLASS.Type = class Rex_SequenceMatcherType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
