"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_text_typing;

	BEHAVIOR_CLASS.Type = class Rex_text_typingType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
