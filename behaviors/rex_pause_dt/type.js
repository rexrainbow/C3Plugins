"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_PauseDt;

	BEHAVIOR_CLASS.Type = class Rex_PauseDtType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
