"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_layouter_cyclic;

	BEHAVIOR_CLASS.Type = class Rex_layouter_cyclicType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
