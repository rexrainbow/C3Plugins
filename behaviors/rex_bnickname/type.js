"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_bNickname;

	BEHAVIOR_CLASS.Type = class Rex_bNicknameType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
