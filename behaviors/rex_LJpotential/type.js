"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_LJ_potential;

	BEHAVIOR_CLASS.Type = class Rex_LJ_potentialType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
