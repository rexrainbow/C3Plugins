"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_boundary;

	BEHAVIOR_CLASS.Type = class Rex_boundaryType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
