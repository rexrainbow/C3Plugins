"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_text_resize;

	BEHAVIOR_CLASS.Type = class Rex_text_resizeType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
