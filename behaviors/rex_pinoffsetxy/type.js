"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_pinOffsetXY;

	BEHAVIOR_CLASS.Type = class Rex_pinOffsetXYType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
